# Core engine — required.
numpy>=1.26
scipy>=1.11
geopandas>=1.0
shapely>=2.0
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
python-dotenv>=1.0

# Optional — only needed to read a real LINZ/GEBCO raster or a Copernicus
# NetCDF subset. The pipeline runs without them on synthetic data.
rasterio>=1.3
xarray>=2024.1
netCDF4>=1.6
